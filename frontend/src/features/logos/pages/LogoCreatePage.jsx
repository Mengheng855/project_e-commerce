import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { createLogo, uploadLogoImage } from '../api/logoApi'
import { LogoForm } from '../components/LogoForm'
export function LogoCreatePage() { const navigate = useNavigate(); const [error, setError] = useState(''); const [isSaving, setIsSaving] = useState(false); async function handleSubmit(values) { setError(''); setIsSaving(true); try { let image = values.image; if (values.image_file) { const uploaded = await uploadLogoImage(values.image_file); image = uploaded?.image ?? uploaded?.url ?? uploaded?.path } const logo = await createLogo({ title: values.title, image, is_active: values.is_active }); navigate('/admin/logos/' + logo.id, { replace: true }) } catch (error) { const first = error?.errors ? Object.values(error.errors).flat()[0] : null; setError(first ?? error?.message ?? 'Could not create logo.') } finally { setIsSaving(false) } } return <div className="grid gap-5"><section className="rounded-lg border border-slate-200 bg-white p-6 shadow-sm"><div className="flex justify-between gap-4"><div><p className="text-sm font-black uppercase tracking-wide text-teal-700">Branding</p><h1 className="mt-2 text-3xl font-black">Add logo</h1><p className="mt-2 text-sm font-medium text-slate-600">Upload a new website logo.</p></div><Link className="h-fit rounded-md border border-teal-800 px-4 py-2 text-sm font-bold text-teal-900 hover:bg-teal-800 hover:text-white" to="/admin/logos">Cancel</Link></div></section>{error ? <div className="rounded-md border border-red-200 bg-red-50 px-4 py-3 text-sm font-semibold text-red-700">{error}</div> : null}<LogoForm isLoading={isSaving} onSubmit={handleSubmit} /></div> }
