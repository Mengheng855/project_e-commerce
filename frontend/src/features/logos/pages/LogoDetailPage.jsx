import { useEffect, useState } from 'react'
import { Link, useParams } from 'react-router-dom'
import { assetUrl } from '../../../shared/utils/assetUrl'
import { getLogo } from '../api/logoApi'
function formatDate(value) { return value ? new Date(value).toLocaleString() : '-' }
export function LogoDetailPage() { const { id } = useParams(); const [logo, setLogo] = useState(null); const [error, setError] = useState(''); useEffect(() => { let active = true; getLogo(id).then((data) => { if (active) setLogo(data) }).catch((error) => { if (active) setError(error?.message ?? 'Could not load logo.') }); return () => { active = false } }, [id]); if (error) return <div className="rounded-md border border-red-200 bg-red-50 p-4 text-sm font-bold text-red-700">{error}</div>; if (!logo) return <div className="rounded-lg border border-slate-200 bg-white p-6 font-bold text-slate-600">Loading logo...</div>; return <div className="grid gap-5"><section className="rounded-lg border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col justify-between gap-4 lg:flex-row lg:items-end"><div><p className="text-sm font-black uppercase tracking-wide text-teal-700">Logo detail</p><h1 className="mt-2 text-3xl font-black">{logo.title ?? 'Logo #' + logo.id}</h1><p className="mt-2 text-sm font-medium text-slate-600">Website logo asset.</p></div><div className="flex gap-3"><Link className="rounded-md border border-teal-800 px-4 py-2 text-sm font-bold text-teal-900 hover:bg-teal-800 hover:text-white" to="/admin/logos">Back</Link><Link className="rounded-md bg-teal-800 px-4 py-2 text-sm font-black text-white hover:bg-teal-900" to={'/admin/logos/' + id + '/edit'}>Update logo</Link></div></div></section><section className="grid gap-5 rounded-lg border border-slate-200 bg-white p-6 shadow-sm lg:grid-cols-[320px_1fr]"><div className="rounded-lg border border-slate-200 p-4"><img alt={logo.title ?? 'Logo'} className="h-40 w-full object-contain" src={assetUrl(logo.image)} /></div><div className="grid content-start gap-4 md:grid-cols-2"><Info label="Title" value={logo.title} /><Info label="Status" value={logo.is_active ? 'Active' : 'Inactive'} /><Info label="Created by" value={logo.created_by?.username ?? '-'} /><Info label="Created at" value={formatDate(logo.created_at)} /><Info label="Updated at" value={formatDate(logo.updated_at)} /></div></section></div> }
function Info({ label, value }) { return <div><p className="text-xs font-black uppercase text-slate-500">{label}</p><p className="mt-1 font-bold text-slate-950">{value || '-'}</p></div> }
